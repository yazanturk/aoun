import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary – dark navy (#0F172A) from design system
  static const primary = Color(0xFF0F172A);
  // Used as gradient complement (slate-800)
  static const primaryDark = Color(0xFF1E293B);

  // Secondary – warm gold (#C5A059) from design system
  static const secondary = Color(0xFFC5A059);

  // Tertiary background (#F8FAFC) from design system
  static const white = Color(0xFFFFFFFF);
  static const lightGray = Color(0xFFF8FAFC);
  static const inputBackground = Color(0xFFF1F5F9);

  // Neutral (#64748B) from design system
  static const mediumGray = Color(0xFF94A3B8);
  static const subText = Color(0xFF64748B);
  static const darkText = Color(0xFF0F172A);

  static const divider = Color(0xFFE2E8F0);
  static const cardShadow = Color(0x0A0F172A);
  static const success = Color(0xFF2ECC71);
  static const error = Color(0xFFE74C3C);
  static const overlay = Color(0x660F172A);
}
